# 단계 3 초기화 소유권·3방향 복구 완료 증거

판정일: 2026-07-19
대상: SIBL-027~032 / SIAC-027~032
최종 판정: **PASS — SIAC-027~032 전부 통과**

## 1. 구현 기준과 변경 범위

- contexa-cli 구현 커밋: `41bbbf2a888331bdf8f88e402fb20177a41ba613`
- macOS 경로 보정 커밋: `e3113498672218e887949c1dce87cba84718e5a7`
- 검증 프리릴리스: `v0.1.2-phase3.2`
- GitHub Actions 실행: `29698210274`
- 실검증 호스트: `D:\tmp\contexa-phase2-host-20260719`
- 실검증 포트: `9080`
- 실검증 PostgreSQL: `contexa-postgres`, `phase2_host_app`, `phase2_contexa`

새 제품 기능은 추가하지 않았다. 기존 `reset.js`에 섞여 있던 소유 Docker 정리와 파일 복구 책임을 `reset-service.js`로 분리했다. `manifest.js`는 manifest 검증, 원본·적용본 snapshot, 설치 transaction만 담당한다. `reset.js`는 명령 해석, 사용자 확인, 결과 출력만 조정한다.

## 2. 자동 검증

대기 셀로 전환된 전체 실행은 증거에서 제외하고 동일 테스트를 즉시 반환하는 묶음으로 나눠 검증했다.

| 검증 범위 | 결과 |
| --- | ---: |
| Phase 3 reset 소유권·3방향 복구 | 26 PASS, 0 FAIL |
| artifact·detector·i18n·Ollama·preflight | 40 PASS, 0 FAIL |
| injector | 42 PASS, 0 FAIL |
| Phase 0 release·manifest·transaction·실제 init/reset | 29 PASS, 0 FAIL |
| side-effect zero | 23 PASS, 0 FAIL |
| stress | 50 PASS, 0 FAIL |
| Phase 4 전용 일반·simulation 공존 | 1개 명시적 SKIP |
| 합계 | 210 PASS, 0 FAIL, 1 SKIP |

추가로 `node --check`는 `init.js`, `reset.js`, `manifest.js`, `reset-service.js`, `reset-phase3.test.js`에서 모두 통과했다. `git diff --check`도 오류가 없다.

## 3. 실제 Docker 초기화 검증

`contexa init --yes --distributed --no-docker`로 manifest와 compose를 만든 뒤 manifest에 기록된 PostgreSQL 자원만 실제 기동하고 일반 `contexa reset --yes`을 실행했다.

- installation ID: `531a717e-a8fc-4915-bdeb-76d0a51092cc`
- compose project: `contexa-phase2-host`
- 소유 label: `io.ctxa.owner=contexa-cli`, `io.ctxa.mode=normal`, installation ID, compose project 모두 일치
- 제거된 대상: manifest에 기록된 정확한 container·volume·network와 검증된 compose 파일
- 보존 대상: 별도 설치 `contexa-postgres` 컨테이너
- 사용자가 init 뒤 추가한 `// phase3 customer change after init`: 보존
- CLI starter 및 distributed dependency: 제거
- `application.yml`: 초기 SHA-256 `1d1fae6384fd1355689a018663c4e46c3a2c4ac89a7f313e30e410d1485d46be`로 복원
- 사용자 변경을 제외한 `build.gradle`: 초기 SHA-256 `f1557dfbd24916d5aa7462194ca688fc254433b25674409b48ceb759f2b04292`와 일치
- manifest·compose·소유 infra directory: 성공 확인 뒤 제거
- 같은 reset 재실행: 종료코드 0, 파일·자원 변경 0건

## 4. 9080 호스트 회귀와 PostgreSQL 실검증

reset 완료 후 Contexa 의존성과 설정이 없는 원래 호스트를 9080에서 다시 기동했다.

| 항목 | 실제 결과 |
| --- | --- |
| 무인증 `GET /host/probe` | HTTP 401 |
| Basic 인증 | HTTP 200 |
| principal | `host-admin` |
| authority | `ROLE_HOST_ADMIN` |
| Authentication 구현 | `UsernamePasswordAuthenticationToken` |
| SecurityFilterChain | `hostSecurityFilterChain` 1개 |
| 필터 | 기존 11개, 순서 동일 |
| Contexa bean | 0개 |
| 종료 후 9080 listener | 없음 |

PostgreSQL은 reset 전후 동일하게 유지됐다.

| 항목 | 실제 값 |
| --- | ---: |
| `phase2_host_app` public table | 0 |
| `phase2_contexa` OSS public table | 137 |
| pgvector extension | 1 |
| `official_metric_purpose_contract` | 12 |
| `official_metric_evaluation_contract` | 66 |
| `official_prompt_signal_contract` | 702 |
| legacy definition/check definition | 0 / 0 |

Phase 3는 CLI·Docker·서버·DB 범위이며 HTML·JS를 변경하거나 요구하지 않으므로 브라우저 화면 판정 항목은 해당 없음이다. HTTP는 실제 9080 서버로 검증했다.

## 5. SIBL·SIAC 1:1 판정

| 백로그 / 완료판정 | 현재 판정 | 직접 증거 |
| --- | --- | --- |
| SIBL-027 / SIAC-027 일반 reset 범위 | PASS | normal manifest만 선택하고 infra=skip이면 Docker 호출 0건, manifest가 없으면 변경 0건, simulation manifest 바이트 보존 |
| SIBL-028 / SIAC-028 ownership guard | PASS | project 밖 경로·`..`·symlink·임의 infra 거부, 정확한 resource ID와 owner·mode·installation·compose label 일치 때만 삭제, 관례명 `contexa-postgres` 보존 |
| SIBL-029 / SIAC-029 3-way reset | PASS | original·CLI-applied·current 3개 상태 저장, 비중첩 사용자 변경 보존, 중첩 변경은 무변경 conflict, 재-init 실패 때 이전 applied snapshot 복원 |
| SIBL-030 / SIAC-030 Docker 실패 시 관리자료 | PASS | CLI 부재·daemon 중지·label 불일치·compose down 실패·부분 삭제 5개 행렬에서 compose·manifest·infra와 남은 자원 보존, non-zero |
| SIBL-031 / SIAC-031 manifest 손상·경로 방어 | PASS | invalid JSON·version·checksum·directory·path escape·snapshot alias·external escape·symlink 거부, manifest 자동 삭제 0건, backup 위치와 재실행 절차 출력 |
| SIBL-032 / SIAC-032 idempotency·감사 결과 | PASS | 반복 reset 무변경, removed·restored·preserved·conflict·failed 구분, 부분실패 성공 문구 없음, manifest 비밀값 `[REDACTED]` |

## 6. 실패 실행 제외와 최종 게이트

- `v0.1.2-phase3.1` 실행 `29697989016`은 macOS unit test 실패이므로 PASS 근거에서 제외한다.
- 실패 후 macOS의 시스템 경로 alias를 과도하게 symlink escape로 판정할 수 있는 상위 경로 순회 guard를 수정했다. 소유 infra directory와 compose 파일 자체의 symlink 거부, manifest exact path, compose checksum, Docker label 검증은 유지했다.
- `v0.1.2-phase3.2` GitHub Actions 실행 `29698210274`: 최종 `success`
- Linux x64·macOS ARM64·Windows x64 build: 모두 `success`
- release job: `success`
- `phase3-acceptance-evidence`: artifact ID `8445651670`, 1,338 bytes, 미만료
- 공개 프리릴리스: `v0.1.2-phase3.2`, draft 아님, prerelease
- 공개 자산: 바이너리 3개, SHA-256 sidecar 3개, release manifest, RSA signature, public key의 총 9개

## 7. 종료 상태

- 9080 listener: 없음
- Phase 3 소유 Docker 자원: 없음
- 무관한 `contexa-postgres`: 정상·보존
- 검증 fixture: 초기 호스트 상태로 원복
- 다음 Phase 진입: **허용**
