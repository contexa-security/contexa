# 단계 4 시뮬레이션 격리·실서버 완료 증거

판정일: 2026-07-20  
판정 대상: SIBL-033~038 / SIAC-033~038  
최종 판정: **PASS**

## 1. 판정 원칙

- `D:\contexa-started`를 실제 사용자 프로젝트로 사용했다.
- 정상 설치와 시뮬레이션 설치의 manifest, 파일, Docker 자원을 분리해 판정했다.
- 애플리케이션은 `contexa simulate run --dir D:\contexa-started` 한 명령으로 실행하고 9080에서 확인했다.
- Docker·HTTP·서버 로그·PostgreSQL을 실제로 확인했다.
- 셸이 cell ID를 반환한 reset 5753과 테스트 재실행 5775·5777은 즉시 종료했고 PASS 증거에서 제외했다.
- 발견된 Kafka 허위 health와 `docker-compose.yml.bak` 잔존은 완료로 처리하지 않고 수정·재설치·재검증했다.

## 2. SIAC 1:1 판정

| ID | 판정 | 실제 증거 |
| --- | --- | --- |
| SIAC-033 | PASS | 정상 installation ID `6ea64b28-f1b3-474a-a997-ceaf1d9a5b2c`와 시뮬레이션 installation ID를 별도로 유지했고 `status`가 둘 다 `COMMITTED`로 표시했다. 공존·독립 reset 실제 계약 테스트 1/1 통과. |
| SIAC-034 | PASS | 시뮬레이션 값은 `application-contexa-sim.yml`과 profile 전용 `ContexaSimulationConfiguration`에만 생성됐다. 정상 build·application.yml·메인 클래스·manifest 해시는 전후 동일했다. |
| SIAC-035 | PASS | project `ctxa-sim`, host port PostgreSQL 25432·Ollama 31434·Redis 26379·Zookeeper 22181·Kafka 29092를 강제했다. 운영 환경변수 오염은 덮어쓰며, 미확인 포트·컨테이너 재사용은 error로 거부한다. |
| SIAC-036 | PASS | 새 프로세스에서 단일 명령으로 `contexa-sim` profile만 활성화했다. 9080 HTTP, Redis 26379, Kafka 29092, Ollama 31434, PostgreSQL 25432 연결을 실제 확인했다. |
| SIAC-037 | PASS | 5개 컨테이너의 owner·mode·installation ID와 health를 검사했다. PostgreSQL 인증 쿼리, Redis PING, Zookeeper TCP, Kafka broker API, Ollama version·model API가 통과한 뒤에만 성공 문구가 출력됐다. |
| SIAC-038 | PASS | 최종 reset 후 시뮬레이션 컨테이너 0, 볼륨 0, 네트워크 0, 생성 파일 0, infra 디렉터리 0이었다. 정상 파일과 `contexa-postgres` digest는 동일했고 두 번째 reset은 종료 0의 명시적 no-op이었다. |

## 3. 실제 설치·반복 설치

시뮬레이션 파일을 모두 제거한 상태에서 다음 명령을 실행했다.

```text
contexa init --simulate --no-docker --yes --dir D:\contexa-started --infra-dir D:\tmp\contexa-phase4-sim-infra
```

첫 설치와 반복 설치가 모두 종료 0이었고 반복 설치 전후 installation ID는 `dbe67a89-9b9d-410d-b877-c954c2c40b3a`로 같았다. manifest의 세 프로젝트 파일은 모두 `generated=true`, `originalChecksum=null`이었다.

- `src/main/resources/application-contexa-sim.yml`
- `src/main/java/io/contexa/contexastarted/ContexaSimulationConfiguration.java`
- `contexa/simulation/data/GeoLite2-City.mmdb`

반복 설치 뒤 `D:\tmp\contexa-phase4-sim-infra\docker-compose.yml.bak`는 존재하지 않았다. 기존 transaction 백업과 Compose injector의 이중 백업이 reset 뒤 잔존하던 결함은 `backupExisting=false` 계약으로 제거했다. injector의 독립 호출 기본 백업 기능은 유지했다.

## 4. Docker 실제 검증

최종 기동에서 다음 5개 컨테이너가 모두 같은 installation ID, `owner=contexa-cli`, `mode=simulation`, `healthy`였다.

| 컨테이너 | host port | 실제 프로토콜 결과 |
| --- | ---: | --- |
| `ctxa-sim-postgres` | 25432 | `contexa_sim\|contexa_sim\|1` 인증 SQL 성공 |
| `ctxa-sim-redis` | 26379 | `PONG` |
| `ctxa-sim-zookeeper` | 22181 | `nc -z localhost 2181` 종료 0 |
| `ctxa-sim-kafka` | 29092 | `kafka:9093 (id: 1 ...)`, ERROR·DisconnectException 0건 |
| `ctxa-sim-ollama` | 31434 | `ollama version is 0.18.2`, model API 종료 0 |

최초 실검증에서는 Kafka health가 `localhost:9092`를 사용해 명령 종료 코드는 0이지만 출력에 `DisconnectException`이 존재했다. 이를 PASS에서 제외하고 내부 리스너 `kafka:9093`으로 변경했으며, CLI가 출력의 `ERROR`와 `DisconnectException`도 거부하도록 수정했다.

## 5. 9080 실서버·DB 검증

`contexa simulate run --dir D:\contexa-started`를 숨김 프로세스로 실행했다.

- 활성 profile: `contexa-sim` 하나
- Tomcat: `9080`, context path `/`
- 기동 시간: 23.261초
- `GET http://127.0.0.1:9080/`: 401 — 서버 도달 및 보안 적용 상태
- Redis 실제 로그: `127.0.0.1:26379`
- Kafka 실제 설정: `bootstrap.servers = [127.0.0.1:29092]`
- Ollama 실제 HTTP: `http://127.0.0.1:31434/api/version` → `0.18.2`
- PostgreSQL: `contexa_sim`, 사용자 `contexa_sim`, public table 137개, vector extension 1개, JDBC connection 9개
- 기동 로그의 `APPLICATION FAILED`, bean cycle, `NoClassDefFoundError`: 0건

시뮬레이션 WebSocket은 전용 overlay에서 비활성화했다. 공통 자동설정은 `PlatformConfig` bean과 `contexa.iam.websocket.enabled=true` 조건을 사용하므로 dependency-only 호스트에는 개입하지 않고, 정상 전역 사용의 기본값은 유지한다.

## 6. 최종 reset inventory

reset 전:

- 컨테이너 5개
- 볼륨 6개: pgdata, ollama-data, redis-data, zookeeper-data, zookeeper-log, kafka-data
- 네트워크 1개
- 시뮬레이션 manifest·overlay·Java configuration·GeoIP·Compose 존재

reset 후:

- 위 컨테이너·볼륨·네트워크·파일·infra 디렉터리: 모두 0건
- 두 번째 `reset --simulate --yes`: 종료 0, `No matching Contexa ownership manifest exists` 출력
- 기존 `contexa-postgres`: ID `d64a024d...7aec4`, image digest `8ed319...5a34`, `running`으로 전후 동일

정상 파일 SHA-256 전후 동일값:

| 파일 | SHA-256 |
| --- | --- |
| `build.gradle` | `14F86D58687E858A053790F669961300946952BFB70E5FE3976338917B265BB3` |
| `application.yml` | `F9C577410BEBD9ED7DC29DC4249E5783D59551CCC963142BE3F910AE9D8C3757` |
| `ContexaStartedApplication.java` | `E2F06CAAC1BBFDBE5384A810FAB05E0346C7E78E81C65F0F0F469AC2E3B107B1` |
| 정상 `contexa/manifest.json` | `406823E63C0E131B2A1A7889B39F7DE8305A008CF5907DAB3865E97007166305` |

## 7. 대상 테스트

- `phase0-contract`: 일반·시뮬레이션 공존·독립 reset 1/1 PASS
- `injector`: 42/42 PASS
- `reset-phase3`: 28/28 PASS
- `sideeffect-zero`: 23/23 PASS
- `preflight`: 6/6 PASS
- `ollama`: 4/4 PASS
- 변경 Java 자동설정 계약: 1/1 PASS
- 변경 JS 문법검사: 9/9 PASS
- `git diff --check`: 오류 0건

cell ID를 만든 마지막 두 테스트 재실행은 위 수치에 추가하지 않았다. 해당 파일의 직전 정상 실행 결과와, 그 뒤 변경된 부분에 대한 단일 회귀 테스트 및 실제 전체 수명주기를 판정 근거로 사용했다.

## 8. 구현 위치

- 시뮬레이션 단일 계약: `contexa-cli/src/core/simulation.js`
- 명령: `contexa-cli/src/commands/init.js`, `simulate.js`, `reset.js`
- overlay·Compose: `contexa-cli/src/core/injector/yml.js`, `compose.js`
- 소유권·복구: `contexa-cli/src/core/manifest.js`, `reset-service.js`
- 사전검사: `contexa-cli/src/core/preflight.js`
- dependency-only WebSocket 경계: `contexa-autoconfigure/.../IamWebSocketAutoConfiguration.java`

Phase 4 범위에서 미구현·부분구현·잔존 시뮬레이션 자원은 없다.
