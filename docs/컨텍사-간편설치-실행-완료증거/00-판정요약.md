# 컨텍사 간편설치·실행 완료판정 요약

판정일: 2026-07-20  
현재 게이트: Phase 7 완료  
Phase 0 판정: **PASS — SIAC-001~005 전부 통과**  
Phase 1 판정: **PASS — SIAC-006~015 전부 통과**  
Phase 2 판정: **PASS — SIAC-016~026 전부 통과**  
Phase 3 판정: **PASS — SIAC-027~032 전부 통과**  
Phase 4 판정: **PASS — SIAC-033~038 전부 통과**  
Phase 5 판정: **PASS — SIAC-039~042 전부 통과**  
Phase 6 판정: **PASS — SIAC-043~045 전부 통과**  
Phase 7 판정: **PASS — SIAC-046~054 전부 통과**  
간편설치·실행 백로그 최종 게이트: **통과**

## 1. SIAC 전체 판정표

| ID | Phase | 판정 | 직접 증거 |
| --- | ---: | --- | --- |
| SIAC-001 | 0 | PASS | [release manifest](../../../contexa-cli/release-manifest.json), [Phase 0 계약 테스트](../../../contexa-cli/test/phase0-contract.test.js), [설치기 테스트](../../../install-ctxa/test/installers.test.js) |
| SIAC-002 | 0 | PASS | 이 문서의 ‘호스트 IAM 비간섭 9080 실검증’, [starter-only 자동설정 테스트](../../../contexa/contexa-autoconfigure/src/test/java/io/contexa/autoconfigure/core/infra/StandaloneAutoConfigurationFilterTest.java) |
| SIAC-003 | 0 | PASS | 이 문서의 ‘공개 릴리스 실제 설치 검증’ |
| SIAC-004 | 0 | PASS | 이 문서의 ‘일반·simulation 소유권 실검증’, [manifest 구현](../../../contexa-cli/src/core/manifest.js) |
| SIAC-005 | 0 | PASS | 이 문서의 ‘원자적 transaction·실패 주입’, [Phase 0 계약 테스트](../../../contexa-cli/test/phase0-contract.test.js) |
| SIAC-006 | 1 | PASS | 서명 manifest의 3개 자산 계약, OS별 GitHub 실제 architecture smoke |
| SIAC-007 | 1 | PASS | bounded HTTP·retry 구현, fake timeout 실제 종료, Docker 사전검사 제거 |
| SIAC-008 | 1 | PASS | 공개 RSA-3072 서명 독립 검증, signed digest와 실제 바이너리 hash 일치 |
| SIAC-009 | 1 | PASS | 동일 파일시스템 임시파일·원자 이동·previous 복구와 실제 rollback 테스트 |
| SIAC-010 | 1 | PASS | 실제 404·timeout·서명·checksum 실패의 non-zero 종료와 기존 바이너리 보존 |
| SIAC-011 | 1 | PASS | 실제 PATH 우선순위 확인, 기존 CLI 미삭제 경고, POSIX 실행권한 계약 |
| SIAC-012 | 1 | PASS | Linux x64·macOS ARM64·Windows x64 실제 build·architecture·SEA smoke |
| SIAC-013 | 1 | PASS | 실제 process 신규·동일버전·update·rollback·uninstall·무인자 실행 |
| SIAC-014 | 1 | PASS | 실제 install.ctxa.ai status·header·path/UA/query·body hash·채널 분리 |
| SIAC-015 | 1 | PASS | `npm test` 9/9, fake release server matrix, CLI release 계약 CI gate |
| SIAC-016 | 2 | PASS | [단계 2 상세 증거](02-단계2-스타터-설치-호스트-비간섭-증거.md), Linux English·Korean pseudo-terminal Enter-only gate |
| SIAC-017 | 2 | PASS | detector 13/13, multi-module·config inventory·Java/Kotlin 집중 검증 |
| SIAC-018 | 2 | PASS | starter-only 9080 실기동, baseline SecurityContext·chain 동일, Contexa bean 0개 |
| SIAC-019 | 2 | PASS | Linux Maven effective POM·Gradle Groovy/Kotlin/buildscript/multi-module compile evidence |
| SIAC-020 | 2 | PASS | 기본 init 설정 파일 byte 보존, 주석·anchor·multi-document·quoting 포함 |
| SIAC-021 | 2 | PASS | SANDBOX/FULL 9080 host IAM 비간섭, Java/Kotlin annotation·rollback 검증 |
| SIAC-022 | 2 | PASS | 사용자 dependency·`spring.ai`·`contexa.llm` 보존과 key provenance 검증 |
| SIAC-023 | 2 | PASS | 6개 mutation 단계·orphan·중단 transaction rollback과 실제 재시도 성공 |
| SIAC-024 | 2 | PASS | artifact 10개 계약과 실제 63,510,258-byte GeoLite2 검증 |
| SIAC-025 | 2 | PASS | 중앙 async 오류 matrix와 Ollama deadline 4/4 검증 |
| SIAC-026 | 2 | PASS | init 무변경 재실행·starter provenance·사용자 변경·reset retry 보존 |
| SIAC-027 | 3 | PASS | [단계 3 상세 증거](03-단계3-초기화-소유권-3방향-복구-증거.md), normal manifest 범위·simulation 상태 byte 보존 |
| SIAC-028 | 3 | PASS | exact path·resource ID·owner·mode·installation·compose label guard와 무관 자원 보존 |
| SIAC-029 | 3 | PASS | original·applied·current 3방향 복구, 비중첩 변경 보존·중첩 conflict·재-init snapshot 복원 |
| SIAC-030 | 3 | PASS | Docker CLI·daemon·label·compose down·부분 제거 실패 행렬과 관리자료 보존 |
| SIAC-031 | 3 | PASS | JSON·version·checksum·directory·path·symlink·snapshot·external escape 거부 |
| SIAC-032 | 3 | PASS | 반복 reset 무변경, 5개 감사 상태, 부분실패 non-zero·성공 문구 없음·비밀값 제거 |
| SIAC-033 | 4 | PASS | [단계 4 상세 증거](04-단계4-시뮬레이션-격리-실서버-증거.md), 정상·시뮬레이션 동시 COMMITTED와 독립 reset |
| SIAC-034 | 4 | PASS | 전용 profile·overlay와 정상 파일 SHA-256 전후 일치 |
| SIAC-035 | 4 | PASS | 고정 project·전용 port·오염 환경변수·충돌 거부 계약 |
| SIAC-036 | 4 | PASS | 단일 명령, contexa-sim 단일 profile, 9080·Redis·Kafka·Ollama 실제 연결 |
| SIAC-037 | 4 | PASS | 5개 healthy·소유권, PostgreSQL·Redis·Zookeeper·Kafka·Ollama 프로토콜 검사 |
| SIAC-038 | 4 | PASS | 시뮬레이션 자원 전체 0건, 정상 digest 동일, 두 번째 reset no-op |
| SIAC-039 | 5 | PASS | [단계 5 상세 증거](05-단계5-구조-다국어-명령상태-증거.md), canonical 실행·기본값·timeout과 duplicate 0건 |
| SIAC-040 | 5 | PASS | init input·plan·diagnostics·report 책임 분리, 기존 mutation·infra service 재사용, dead cleanup 제거 |
| SIAC-041 | 5 | PASS | 426개 bundle key parity, PS5.1·PS7·POSIX 실제 한국어 출력, raw key·U+FFFD 0건 |
| SIAC-042 | 5 | PASS | 여섯 설치 상태·exit matrix, doctor·simulate 실패 전파, mode provenance |
| SIAC-043 | 6 | PASS | 실제 자식 프로세스 7개 상태 전이, 7 PASS·0 FAIL·0 SKIP |
| SIAC-044 | 6 | PASS | 9080 dependency-only 호스트 보안 동일, simulation HTTP·PostgreSQL·Redis·Kafka·Ollama 실제 검증 |
| SIAC-045 | 6 | PASS | CLI Linux·macOS·Windows release와 설치기 Windows·Ubuntu·macOS 실제 설치 상태전이 전부 success |
| SIAC-046 | 7 | PASS | [단계 7 상세 증거](07-단계7-실제설치-리셋-스키마복구-증거.md), Maven·Gradle 분산 build backup·byte reset·실패 주입 |
| SIAC-047 | 7 | PASS | manifest 없는 반복 reset no-op, exact Docker ownership, 운영 PostgreSQL fingerprint 불변 |
| SIAC-048 | 7 | PASS | CLI SQL 0건, OSS 139·Enterprise 162+1 inventory, PostgreSQL·9080 실검증 |
| SIAC-049 | 7 | PASS | OSS 단독 9080, metric 12·check 26, key digest·재기동 불변 |
| SIAC-050 | 7 | PASS | OSS provenance handoff, SQL migration 154, 미등록 DB 무변경 거부, 9080 200 |
| SIAC-051 | 7 | PASS | 빈 Enterprise DB 단일 coordinator·재기동, product digest 불변, ERROR 0 |
| SIAC-052 | 7 | PASS | OSS·Enterprise·HOST·LEGACY_UNOWNED versioned inventory와 set diff |
| SIAC-053 | 7 | PASS | 현재 snapshot installer exit 0, 실제 설치 binary init·9080·PostgreSQL·reset byte 복구 |
| SIAC-054 | 7 | PASS | 실제 타 소유 서비스·HTTP decoy 사전 거부, 파일 변경 0, preflight 9/9 |

## 2. SIAC-001 네 핵심 명령 계약

판정: **PASS**

- canonical manifest의 핵심 명령은 `contexa init`, `contexa reset`, `contexa init --simulate`, `contexa reset --simulate` 네 개다.
- CLI 무인자 help, README, Windows·POSIX 설치 완료 안내가 같은 네 명령을 사용한다.
- 실제 child process로 일반 init/reset/반복 reset, simulation init/reset, 실패 init을 실행했다.
- `status`, `doctor`, `scan`은 보조 명령이며 기본 설치 전제조건이 아니다.

검증 결과:

- Phase 0 계약 테스트: 13개 전체 통과 후 마지막 infra-root assertion 대상 테스트 1개 재확인 통과
- injector·side-effect 회귀 테스트: 56/56 PASS
- install-ctxa 집중 테스트: 5/5 PASS
- diff 검사 오류: 0건

## 3. SIAC-002 호스트 IAM 비간섭 9080 실검증

판정: **PASS**

동일한 Spring Boot 3.5.4 호스트 fixture와 동일한 PostgreSQL을 사용해 baseline과 starter-only를 각각 9080에서 기동했다.

| 항목 | baseline | starter-only |
| --- | --- | --- |
| SecurityFilterChain bean | `hostSecurityFilterChain` 1개 | 동일 |
| chain 내부 security filter | Spring Security 표준 filter 12개 | 클래스·순서까지 동일 |
| AuthenticationManager bean | 0개 | 동일 |
| AuthenticationProvider bean | 0개 | 동일 |
| Contexa bean | 0개 | 0개 |
| principal | `host-admin` | 동일 |
| authenticated | `true` | 동일 |
| authorities | `ROLE_HOST_ADMIN` | 동일 |
| Authentication 구현 | `UsernamePasswordAuthenticationToken` | 동일 |
| 무인증 `/host/probe` | 401 | 401 |
| 인증 `/host/probe` DB probe | 1 | 1 |
| starter-only 기동 후 public schema table | 해당 없음 | 0개 |

starter-only 서버는 `Started Phase0HostApplication in 5.429 seconds`로 정상 기동했고 `APPLICATION FAILED`, `NoClassDefFoundError`, bean cycle, ERROR가 없었다.

CLI 실제 init에서도 기존 `spring.security`, `spring.datasource`, `spring.ai` 값이 의미적으로 동일했고 reset 후 build·YAML 전체 digest가 원본과 같았다.

## 4. SIAC-003 공개 릴리스 실제 설치 검증

판정: **PASS**

2026-07-19 실제 확인값:

- GitHub release workflow run: 29689524195, conclusion success
- GitHub release API 최신 tag: `v0.1.1`
- 공개 시각: `2026-07-19T13:48:09Z`
- 공개 자산: Linux x64, macOS ARM64, Windows x64 binary와 각 checksum, `release-manifest.json`
- 공개 manifest의 `cliVersion`: `0.1.1`
- 공개 manifest의 starter: `ai.ctxa:spring-boot-starter-contexa:0.1.0-SNAPSHOT`
- 실제 install.ctxa.ai Windows installer 실행: 성공
- 실제 격리 설치 binary의 `contexa --version`: `0.1.1`
- 공개 checksum과 설치 binary SHA-256: `459b66fb2dfd4354d56aec83c5b7a6a536360eb3f9f508879847ce1850c92542`, 일치
- 설치 binary가 주입한 starter: `0.1.0-SNAPSHOT`
- 설치 binary의 init manifest CLI version: `0.1.1`
- init 전후 호스트 spring.security, spring.datasource, spring.ai: 동일
- reset 후 build.gradle·application.yml SHA-256: 사전값과 동일

첫 실 installer 검증에서는 Windows 임시 binary가 `.tmp` 확장자여서 version 실행이 실패했다. `install.ps1`을 실행 가능한 `.exe` 임시 경로로 수정하고 commit `23721bb`를 배포한 뒤 동일 공개 v0.1.1 자산으로 재검증해 통과했다. 실패 실행은 PASS 근거로 사용하지 않았다.

## 5. SIAC-004 일반·simulation 소유권 실검증

판정: **PASS**

- manifest path, backup root, installation ID, infra root는 mode별로 분리됐다.
- 모든 manifest file entry에 mode와 installation ID가 기록된다.
- `status`가 일반·simulation 동시 존재를 각각 `COMMITTED`로 표시하고 simulation reset 후 일반만 남았음을 표시했다.
- 실제 `init --simulate --no-docker` installation ID: `252df4a7-de8a-4b96-959a-e47a49d844ed`
- 생성 compose로 격리 프로젝트 `ctxa-phase0-labels`의 PostgreSQL만 실행해 실제 label을 검사했다.

| Docker 자원 | owner | mode | installation ID |
| --- | --- | --- | --- |
| container | `contexa-cli` | `simulation` | `252df4a7-de8a-4b96-959a-e47a49d844ed` |
| volume | `contexa-cli` | `simulation` | 동일 |
| network | `contexa-cli` | `simulation` | 동일 |

검증 후 container·volume·network는 모두 제거됐고 simulation manifest도 제거됐다. fixture의 build와 application.yml은 사전 SHA-256과 일치했다.

검증 투명성: 실 Docker 검증의 첫 reset은 검증기가 삭제 대상 infra 디렉터리 자체를 현재 작업 디렉터리로 둔 탓에 Windows EBUSY로 non-zero 종료했다. 이 실행은 PASS 근거로 사용하지 않았다. 해당 실행 전 파일·manifest 복구는 완료됐고, 프로젝트 루트에서 수행한 command-level simulation reset은 별도로 통과했다. 검증기가 만든 infra 잔존물은 정확한 절대경로 확인 후 제거했다. cleanup 실패 뒤 manifest가 먼저 제거되는 재시도성은 후속 reset 완료판정에서 다시 검증할 대상이며 숨기지 않는다.

## 6. SIAC-005 원자적 transaction·실패 주입

판정: **PASS**

- plan은 파일의 CREATE/MODIFY, 외부 COPY/DOWNLOAD, Docker START/SKIP/NONE, DELETE NONE을 실제 write 전에 출력한다.
- manifest는 임시 파일 작성 후 rename하는 원자적 교체 방식이며, rename 실패 주입 시 마지막 유효 manifest가 byte 단위로 보존됐다.
- build, YAML 설정, Java, 생성 data, 외부 compose를 한 transaction에서 모두 변경한 뒤 실패를 주입해 기존 파일은 원본 byte로 복구되고 생성 data는 삭제됐다.
- 백업 직후 manifest record 전 실패 경로도 orphan backup 검색으로 복구됐다.
- rollback 자체 실패 시 `ROLLBACK_FAILED`와 오류·복구 metadata가 보존되고 성공으로 보고되지 않았다.
- `IN_PROGRESS` 상태에서 중단된 뒤 다음 init transaction을 시작하면 기존 project 파일과 외부 compose를 먼저 복구하고 새 transaction ID를 발급했다.
- 실제 잘못된 YAML을 사용한 `contexa init`은 non-zero로 실패했고 build·YAML 원본, `ROLLED_BACK` 상태를 확인했다.

## 7. Phase 1 설치 안전성·배포 실검증

판정: **PASS — SIAC-006~015 전부 통과**

배포 식별자:

- install-ctxa 핵심 설치 안전성 commit: `82b394b`
- install-ctxa stable channel 최종 교정 commit: `a52f70b`
- contexa-cli signed release commit: `e30252a`
- prerelease tag: `v0.1.2-phase1.1`
- GitHub Actions run: [29691304078](https://github.com/contexa-security/contexa-cli/actions/runs/29691304078), conclusion `success`
- 공개 시각: `2026-07-19T14:42:05Z`

### 7.1 SIAC-006·012 지원 행렬과 실제 산출물

signed manifest와 installer가 허용하는 행렬은 Linux x64/glibc 2.28+, macOS ARM64/macOS 11+, Windows x64/Windows 10+ 세 개뿐이다. Linux ARM64, Intel Mac, Windows ARM64는 파일·PATH 변경 전에 거부한다.

공개 prerelease에는 binary 3개, SHA-256 sidecar 3개, release manifest, RSA signature, public key의 총 9개 자산이 존재한다. GitHub Actions의 다음 실제 단계가 모두 성공했다.

| job | architecture·SEA smoke | code signature 계약 |
| --- | --- | --- |
| Build / linux-x64 | success | unsigned-snapshot |
| Build / macos-arm64 | success | ad-hoc codesign verify |
| Build / win-x64 | success | PE machine `0x8664`, Authenticode `NotSigned` |

### 7.2 SIAC-007·010 bounded 실패와 종료 코드

- PowerShell은 connect 5초, 요청 total 30초, 제한 retry 2회를 기본값으로 사용한다.
- POSIX curl은 connect timeout, max-time, 제한 retry, retry-max-time을 함께 적용한다.
- installer는 Docker를 검사하거나 기다리지 않는다.
- fake release server의 404, timeout, 서명 불일치, checksum 불일치에서 실제 PowerShell process는 모두 non-zero로 종료했다.
- 네 실패 모두 사전 설치 binary의 SHA-256가 그대로 보존됐다.
- outer pipeline의 다운로드 실패와 installer 실패는 README의 명령으로 호출자에게 non-zero를 반환한다.

### 7.3 SIAC-008·009 서명·digest·원자 교체

공개 `release-manifest.json.sig`를 공개 `release-signing-public.pem`으로 별도 OpenSSL 검증한 결과는 `Verified OK`였다. signing private key는 GitHub Actions secret에만 등록했고 작업 파일은 즉시 삭제했으며 변경 파일 private-key 검색 결과는 0건이었다.

실제 Windows 설치값:

| 항목 | 값 |
| --- | --- |
| CLI version | `0.1.2-phase1.1` |
| manifest signature | required, `RSA-3072-SHA256` |
| manifest Windows SHA-256 | `0a19ceef269913b93862ce0af9402109d0978a7737d3d591cb6697d2264f49de` |
| 설치 binary SHA-256 | 동일 |
| Authenticode | `NotSigned`, snapshot 계약과 일치 |
| `--help` / 무인자 종료 | 0 / 0 |

임시 binary는 최종 설치 디렉터리와 같은 파일시스템에 만들고 smoke 성공 후 atomic move한다. 기존 binary는 `.previous`로 이동하며 최종 smoke 실패 시 복구한다. 실제 process lifecycle 테스트에서 신규 설치, 동일 버전 무교체, update, rollback, uninstall을 모두 실행해 통과했다.

### 7.4 SIAC-011·013 PATH와 수명주기

- 격리 public install에서 설치 binary가 PATH의 첫 `contexa`로 해석됐다.
- 기존 사용자 경로의 다른 CLI를 발견했지만 삭제하지 않고 경고했다.
- 동일 버전 재설치의 SHA-256와 LastWriteTime이 모두 동일했고 `.previous`를 생성하지 않았다.
- uninstall은 설치 binary와 installer-owned PATH만 제거했다.
- 실제 제거 후 별도의 project marker 내용은 그대로 보존됐다.
- `contexa reset`과 CLI uninstall은 문서와 출력에서 서로 다른 작업으로 구분된다.

### 7.5 SIAC-014 실제 설치 엔드포인트

stable과 prerelease를 혼합하지 않았다.

| 요청 | 실제 결과 |
| --- | --- |
| `/install.sh` | 307, 검증된 Phase 0 installer commit `23721bb0e7b561a7deb356656a7e2e3a879bc086/install.sh` |
| `/` + WindowsPowerShell UA | 302, 같은 stable commit의 `install.ps1`, `no-store` |
| `/v0.1.2-phase1.1/install.ps1` | 302, 같은 prerelease tag, `public, max-age=31536000, immutable` |

리다이렉트 후 실제 최종 응답은 status 200, `text/plain; charset=utf-8`, `X-Content-Type-Options: nosniff`였다. 명시 `install.sh`에 WindowsPowerShell UA와 query를 넣어도 shell을 반환했고, 명시 `install.ps1`에 curl UA와 query를 넣어도 PowerShell을 반환했다.

- stable shell body SHA-256: `fff46c91820ba019e90055e4e014c8f02d90601a7c438de3b60a9904ccfcb9d7`
- stable PowerShell body SHA-256: `c8cf5c3e4086bb6fa77a54cb91e771c1a22df0c119f76bbd219f5abf3630255a`
- source read 실패 주입의 503 body는 실행 가능한 성공 script가 아니며 `no-store`다.

### 7.6 SIAC-015 자동화 게이트

- 표준 명령: install-ctxa에서 `npm test`
- 최종 결과: 9/9 PASS
- 포함 범위: parser, BOM, 정적 계약, Vercel channel/header, API path·UA·query·503, 실제 Windows lifecycle, 실제 실패 exit·보존, Git Bash POSIX signed install
- contexa-cli Phase 1 release contract test는 asset 이름, OS/CPU, checksum file, 3072-bit public key, workflow 서명·digest binding을 검사하며 release CI의 `npm test` gate에서 성공했다.

검증 투명성:

- 첫 공개 installer 종합 명령은 89MB binary 다운로드와 후속 재설치를 한 프로세스에 묶어 30초 상한을 넘겼으므로 즉시 종료했고 PASS 근거로 사용하지 않았다. 생성된 binary는 별도 명령에서 공개 hash·version·smoke를 검증했고 동일 버전 재실행도 별도 bounded 명령으로 통과했다.
- stable channel을 존재하지 않는 `install-ctxa/v0.1.1` 태그로 가리킨 첫 응답은 결함으로 판정하고 PASS 근거에서 제외했다. 실제 존재와 body를 확인한 전체 commit SHA `23721bb0e7b561a7deb356656a7e2e3a879bc086`로 교정한 후 HTTP matrix를 다시 통과했다.

## 8. Phase 2 starter 설치·호스트 비간섭 실검증

판정: **PASS — SIAC-016~026 전부 통과**

- contexa-cli commit: `1aa5ce83b474fe3064e7622fb68094324472abc2`
- platform commit: `c3ee3dda`
- prerelease: [v0.1.2-phase2.2](https://github.com/contexa-security/contexa-cli/releases/tag/v0.1.2-phase2.2)
- GitHub Actions: [29696154854](https://github.com/contexa-security/contexa-cli/actions/runs/29696154854), Linux·macOS·Windows·release 전부 success
- Linux evidence artifact: `phase2-acceptance-evidence`, 5,229 bytes, 미만료
- 로컬 CI 동일 단위 결과: 184 PASS, 0 FAIL, Phase 4 전용 1 SKIP
- 9080 baseline 대비 starter-only·SANDBOX·FULL의 principal, authority, Authentication 구현, host chain, 11개 filter 순서 동일
- 실제 PostgreSQL: pgvector 1, OSS public table 137, 목적 계약 12, 평가 계약 66, prompt signal 702

실패한 `v0.1.2-phase2.1`과 최초 FULL·SANDBOX·reset 실행은 PASS 근거에서 제외했다. 원인을 구현에서 교정한 뒤 새 태그·새 서버 기동·새 reset으로 재검증했다. 상세 내용은 [단계 2 starter 설치·호스트 비간섭 완료 증거](02-단계2-스타터-설치-호스트-비간섭-증거.md)에 기록했다.

## 9. Phase 3 소유권 기반 reset 실검증

판정: **PASS — SIAC-027~032 전부 통과**

- contexa-cli 구현 commit: `41bbbf2a888331bdf8f88e402fb20177a41ba613`
- macOS 경로 보정 commit: `e3113498672218e887949c1dce87cba84718e5a7`
- prerelease: [v0.1.2-phase3.2](https://github.com/contexa-security/contexa-cli/releases/tag/v0.1.2-phase3.2)
- GitHub Actions: [29698210274](https://github.com/contexa-security/contexa-cli/actions/runs/29698210274), Linux·macOS·Windows·release 전부 success
- `phase3-acceptance-evidence`: artifact ID `8445651670`, 1,338 bytes, 미만료
- 공개 release 자산: 9개
- 로컬 분할 게이트: 210 PASS, 0 FAIL, Phase 4 전용 1 SKIP
- 실제 Docker reset: manifest 소유 container·volume·network만 제거, 무관 `contexa-postgres` 보존
- 3방향 복구: CLI 변경 제거, 사용자 후속 변경 보존, 중첩 변경은 무변경 conflict
- 9080 reset 후 호스트: 무인증 401, 인증 200, host principal·chain·11개 filter 동일, Contexa bean 0개
- PostgreSQL: OSS 137 tables, vector 1, 계약 12·66·702, legacy 0·0 유지

실패한 `v0.1.2-phase3.1` run은 PASS 근거에서 제외했다. macOS 시스템 경로 alias 오탐을 수정한 새 tag에서 세 OS와 release를 다시 검증했다. 상세 내용은 [단계 3 초기화 소유권·3방향 복구 완료 증거](03-단계3-초기화-소유권-3방향-복구-증거.md)에 기록했다.

## 10. 정리 상태

- 9080 listener: 없음
- Phase 0 PostgreSQL 검증 컨테이너: 제거됨
- `ctxa-phase0-labels` container·volume·network: 제거됨
- simulation manifest: 제거됨
- 검증 fixture build·application.yml: 사전 SHA-256로 복구됨
- Phase 1 격리 public install binary·backup: uninstall로 제거됨
- Phase 1 검증용 signing private key: 삭제됨
- stable 기본 channel: 검증된 Phase 0 installer commit으로 보존됨
- Phase 1 prerelease channel: immutable tag로 분리됨
- Phase 2 검증 fixture: baseline으로 복구됨
- Phase 2 PostgreSQL 검증 DB: 증거 보존을 위해 유지
- Phase 3 소유 Docker 자원: 제거됨
- 무관한 `contexa-postgres`: 정상·보존
- Phase 3 검증 fixture: baseline으로 복구됨
- Phase 5: 완료
- Phase 6: 완료
- Phase 6 격리 installer: 공식 uninstall 후 제거됨

## 11. Phase 5 구조·다국어·명령 상태

판정: **PASS — SIAC-039~042 전부 통과**

- contexa-cli 구현 commit: `c442b55`
- 안정 token 교정 commit: `a73c298`
- prerelease: [v0.1.2-phase5.2](https://github.com/contexa-security/contexa-cli/releases/tag/v0.1.2-phase5.2)
- contexa-cli GitHub Actions run: [29705261453](https://github.com/contexa-security/contexa-cli/actions/runs/29705261453), Linux·macOS·Windows·release success
- install-ctxa commit: `6420482`
- install-ctxa GitHub Actions run: [29705006445](https://github.com/contexa-security/install-ctxa/actions/runs/29705006445), Ubuntu·Windows success
- 공개 release 자산 9개, CLI `0.1.2-phase5.2`, starter `0.1.0-SNAPSHOT`, 세 SHA-256와 RSA-3072 서명 계약 일치
- 실패한 `v0.1.2-phase5.1` run은 PASS 근거에서 제외하고 안정 token 교정 후 새 tag 전체 게이트를 다시 통과했다.

상세 내용은 [단계 5 구조·다국어·명령 상태 완료 증거](05-단계5-구조-다국어-명령상태-증거.md)에 기록했다.

## 12. Phase 6 상태전이·실서버·교차 플랫폼

판정: **PASS — SIAC-043~045 전부 통과**

- contexa-cli commit: `9989fc67109efc73c5eb23a743566db5858ab241`
- prerelease: [v0.1.2-phase6.3](https://github.com/contexa-security/contexa-cli/releases/tag/v0.1.2-phase6.3)
- contexa-cli GitHub Actions run: [29707326350](https://github.com/contexa-security/contexa-cli/actions/runs/29707326350), Linux·macOS·Windows·release success
- install-ctxa commit: `a3b08ba394dc117a29415d126ee3d79670f13d3e`
- install-ctxa GitHub Actions run: [29707961048](https://github.com/contexa-security/install-ctxa/actions/runs/29707961048), Windows·Ubuntu·macOS success
- 네 핵심 명령 실제 자식 프로세스 상태전이: 7 PASS, 0 FAIL, 0 SKIP
- dependency-only 9080 설치 전후: principal, authority, Authentication 구현, host chain, 11개 filter, 401/200 결과 동일, Contexa bean 0개
- 명시적 simulation: PostgreSQL·Redis·Kafka·Ollama 실제 연결, 인증 endpoint 200, health 200 `UP`, OSS table 137개
- 공개 release 자산 9개, CLI `0.1.2-phase6.3`, starter `0.1.0-SNAPSHOT`, RSA-SHA256 signature 검증 PASS
- 실패한 중간 run과 대기 셀은 PASS 근거에서 전부 제외하고 원인 교정 후 새 전체 게이트를 통과했다.

상세 내용은 [단계 6 상태전이·실서버·교차 플랫폼 완료 증거](06-단계6-상태전이-실서버-교차플랫폼-증거.md)에 기록했다.


## 13. Phase 7 실제 설치·reset·schema 복구

판정: **PASS — SIAC-046~054 전부 통과**

- 변경 범위 대상 테스트: CLI reset 34/34, preflight 9/9, OSS Java 31/31, Enterprise schema 3/3 PASS
- 현재 snapshot Windows x64 SEA를 로컬 서명 release 서버와 `install.ps1`로 격리 설치: installer exit 0
- source·설치 binary SHA-256 일치: `1ee6e19d40f35804bfa36fb72b35ad603cc541c55f91df0f237fa583ec7936c1`
- `D:\contexa-started` 실제 설치 binary init·9080·PostgreSQL·reset과 기준 파일 byte 복구 PASS
- OSS 단독 139 table, 공식검사 metric 12·check 26, 재기동 불변
- OSS→Enterprise handoff와 빈 Enterprise DB 모두 SQL migration 154개, 제품 object digest 일치
- 미등록 non-empty DB는 schema 변경 전 거부되고 기존 두 table 보존
- HOST `orders`와 `LEGACY_UNOWNED official_verification_oss_run`은 제품 inventory와 분리
- 타 소유 정상 서비스와 HTTP decoy는 파일 쓰기·Docker up 전에 non-zero 거부

상세 내용은 [단계 7 실제 설치·리셋·스키마 복구 완료 증거](07-단계7-실제설치-리셋-스키마복구-증거.md)에 기록했다.

