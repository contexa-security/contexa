# 단계 5 구조·다국어·명령 상태 완료 증거

판정일: 2026-07-19  
대응 백로그: SIBL-039~042  
대응 완료판정: SIAC-039~042  
현재 판정: **PASS — SIAC-039~042 전부 통과**

Phase 5는 신규 사용자 기능을 추가하지 않았다. 기존 `init`, `reset`, `simulate`, `doctor`, `ollama`, `status`, `mode`의 공개 계약을 유지하면서 중복 실행 경로를 단일화하고 상태·종료 코드·다국어·소유권 검사를 보강했다.

## 1. 배포 식별자

- contexa-cli 최초 구현 commit: `c442b55`
- contexa-cli 안정 token 교정 commit: `a73c298`
- contexa-cli 최종 후보 tag: `v0.1.2-phase5.2`
- contexa-cli 최종 후보 GitHub Actions run: `29705261453`
- install-ctxa commit: `6420482`
- install-ctxa GitHub Actions: `29705006445`, Ubuntu·Windows 모두 success
- starter version: `ai.ctxa:spring-boot-starter-contexa:0.1.0-SNAPSHOT`
- CLI package, release tag, release manifest CLI version: `0.1.2-phase5.2`로 일치

## 2. SIAC-039 공통 구성요소 단일화

판정: **PASS**

canonical 구현은 다음과 같다.

| 계약 | canonical 구현 | 주요 소비자 |
| --- | --- | --- |
| Docker 실행·오류·기본 timeout | `src/core/docker.js` | init, reset, simulate, doctor, ollama, preflight |
| host port·simulation port·image·개발 credential | `src/core/infrastructure.js` | compose, YAML injector, init, doctor, preflight, simulation, scan |
| provider 정규화·Ollama model 검증 | `src/core/provider.js` | init input, doctor, ollama, YAML injector |
| 실행 timeout | `src/core/timeouts.js` | Docker, HTTP artifact, Java probe, Ollama, simulation |
| HTTP artifact download | `src/core/artifact.js#downloadToFile` | init artifact provision |
| Ollama pull·감지·준비 확인 | `src/core/ollama.js` | init, ollama command |
| port bind 검사 | `src/core/preflight.js#isPortBound` | init preflight, doctor |

제한 검색과 계약 테스트 결과:

- `downloadToFile` 정의: 1개
- `dockerTry` 정의: `src/core/docker.js` 1개
- `pullOllamaModelWithProgress` 정의: `src/core/ollama.js` 1개
- reset의 사설 `dockerTry`: 0개
- command-local Ollama pull·감지 구현: 0개
- 개발 DB credential literal: canonical 모듈 1개, 생성 template은 해당 상수만 참조
- host/simulation port default: canonical 모듈 1개, 생성 template과 명령은 해당 상수만 참조
- Java 진단: shell 문자열을 제거하고 `spawnSync('java', ['-version'])`와 공통 5초 timeout 사용
- 미확인 점유 port: 시작 모드에서 오류, 서비스 재사용 0건
- 동명 비소유 container: `owner`, `mode`, Compose project 불일치 시 오류, 재사용 0건
- 검증된 소유 container만 `servicesToUp`에서 제외

대상 테스트:

- preflight 8/8 PASS
- Ollama 5/5 PASS
- canonical provider·port·credential·timeout 계약 PASS
- 프로젝트명 환경변수 우선순위와 manifest 프로젝트 명시 조회 PASS
- 중복·dead code 계약 PASS

## 3. SIAC-040 init 책임 분리·dead code 제거

판정: **PASS**

책임·의존성 구조:

```text
commands/init.js                  command 등록·전체 transaction owner
  ├─ core/init-input.js           flag 기본값·interactive prompt·입력 검증
  ├─ core/init-plan.js            경로 정규화·변경 계획·추적 파일 상태
  ├─ core/init-diagnostics.js     Java·Docker 사전 진단
  ├─ core/injector/*              기존 project mutation 서비스 재사용
  ├─ core/artifact.js             기존 외부 artifact provision 재사용
  ├─ core/preflight.js            infra 충돌·소유권 사전검사
  ├─ core/docker.js               infra process 실행
  ├─ core/manifest.js             transaction·provenance·rollback owner
  └─ core/init-report.js          최종 결과·다음 단계 보고
```

- mutation과 infra를 위해 새 중복 서비스를 만들지 않고 기존 injector, artifact, Docker, manifest를 재사용했다.
- `init.js` 내부 prompt, path normalization, tracked state, plan 출력, 진단, 최종 report 구현을 분리했다.
- 광범위 삭제 경로만 가진 미사용 `src/core/cleanup.js`는 import graph 0건 확인 후 삭제했다.
- 사용하지 않는 `inquirer`, command-local provider/Docker/Ollama 구현과 import를 제거했다.
- 공개 command·option 추가: 0건
- 단순 line count를 완료 기준으로 사용하지 않았다.

회귀 증거:

- 실제 starter-only `init --check`, `init --yes`, `reset --yes`: PASS
- init 전후 host application YAML byte 동일
- auto-annotate provider 미지정: `AUTO_ANNOTATE_PROVIDER_REQUIRED`, exit 1
- mode는 unowned YAML을 거부하고 CLI-owned managed path만 transaction으로 변경

## 4. SIAC-041 다국어·UTF-8

판정: **PASS**

- 영어·한국어 bundle: 426개 key 1:1 일치
- init, reset, simulate, doctor, ollama의 help·중요 성공·실패 메시지는 동일 `t(key)` 경로 사용
- stable 오류 코드와 상태 token은 번역하지 않음
- 영어·한국어 help에서 raw key: 0건
- 실제 한국어 starter-only init/reset 출력의 raw key: 0건
- 한국어 bundle과 실제 출력의 U+FFFD: 0건
- install.ps1은 no-BOM PowerShell 5.1 source 호환을 위해 한국어 resource를 UTF-8 Base64로 복원하며, stdout/stderr를 UTF-8(no BOM)으로 고정
- install.sh는 UTF-8 literal message catalog 사용
- PowerShell 5.1 한국어 uninstall·invalid action: 2/2 PASS
- Git Bash POSIX 한국어 uninstall·invalid action: 2/2 PASS
- 로컬에는 `pwsh`가 없어 PowerShell 7 테스트는 SKIP이며 로컬 PASS로 계산하지 않았다.
- install-ctxa run `29705006445`에서 Ubuntu POSIX와 Windows PowerShell 5.1·7·Git Bash job이 모두 success다.

## 5. SIAC-042 명령 의미·상태·종료 코드

판정: **PASS**

`contexa status` 실제 child process 판정:

| 상태 | token | exit |
| --- | --- | ---: |
| 미설치 | `UNINSTALLED` | 0 |
| 일반 설치 | `NORMAL` | 0 |
| simulation 설치 | `SIMULATION` | 0 |
| 동시 설치 | `BOTH` | 0 |
| 손상·소유권 충돌 | `CONFLICT` | 1 |
| 미완료 transaction | `PARTIAL_FAILURE` | 1 |

추가 계약:

- doctor invalid provider: `INVALID_PROVIDER`, exit 1
- doctor 진단 실패: `DOCTOR_CHECK_FAILED`, non-zero
- simulate child spawn error: 원인 전파
- simulate child non-zero: `SIMULATION_DOCKER_FAILED`와 실제 status 전파
- mode: normal COMMITTED manifest, managed path, last CLI checksum이 모두 일치할 때만 변경
- init/reset/simulate/doctor/ollama 영어·한국어 help 기본값과 canonical 실제 기본값 일치

## 6. 실행한 로컬 판정 명령

전체 테스트를 실행하지 않고 Phase 5 변경 범위만 실행했다.

- `node --test test/preflight.test.js` → 8 PASS
- `node --test test/ollama.test.js` → 5 PASS
- Phase 5 계약을 세 묶음으로 실행 → 5 PASS, 2 PASS, 2 PASS
- 변경된 Phase 0 init default 계약 → 1 PASS
- 변경된 side-effect 프로젝트명 계약 → 1 PASS
- install-ctxa 한국어 runtime → PowerShell 5.1 PASS, POSIX PASS, 로컬 PS7 SKIP
- `node --check` 대상 구현 → PASS
- 영어·한국어 JSON parse·key parity → 426/426
- 두 저장소 `git diff --check` → 오류 0건

## 7. 제외한 실행

다음 실행은 `Script running with cell ID`를 반환해 즉시 종료했으며 PASS 증거에서 제외했다.

- 셀 `5895`: Phase 5 테스트 묶음
- 셀 `6013`: 파일 조회
- 셀 `6107`: 저장소 상태 조회
- 셀 `6114`: Phase 5 전체 계약 파일

셀 `6114`는 종료 시점에 출력상 9개 PASS를 반환했지만 정책대로 사용하지 않았다. 동일 계약은 이름별 세 묶음의 독립 실행으로 다시 통과했다.

## 8. 최종 판정 결과

다음 두 GitHub 실행이 모두 success이고 CLI release 자산 9개가 생성됐으므로 SIAC-039~042와 Phase 5를 PASS로 판정한다.

1. install-ctxa `Phase acceptance` run `29705006445`: Ubuntu·Windows success
2. contexa-cli `v0.1.2-phase5.2` Release run `29705261453`: Linux x64·macOS ARM64·Windows x64·release success

공개 prerelease는 2026-07-19T21:58:57Z에 생성됐다. binary 3개, checksum 3개, release manifest, RSA signature, public key의 9개 자산이 존재한다. 공개 manifest는 CLI `0.1.2-phase5.2`, starter `0.1.0-SNAPSHOT`, 세 asset의 64자리 SHA-256, `RSA-3072-SHA256` 필수 서명 계약을 포함한다.

## 9. 실패 실행과 교정

`v0.1.2-phase5.1` Release run `29705039070`은 Windows·macOS build는 성공했지만 Linux의 기존 Phase 2 interactive acceptance에서 실패했고 release job은 skipped였다. 이 실행은 PASS 근거에서 제외한다.

원인은 한국어 plan을 현지화하면서 자동화 계약인 `SETUP: QUICK`, `INTEGRATION: MERGE`, `HOST CONFIG: NONE`, `DOCKER: NONE`, `EXTERNAL DOWNLOAD: NONE` token까지 번역한 것이다. 한국어 설명은 유지하고 다섯 안정 token을 앞에 복원했으며, 두 locale 모두 동일 token을 포함하는 회귀 assertion을 추가했다. 교정본은 새 `phase5.2` tag에서 전체 릴리스 게이트를 다시 통과해야 한다.

교정본 run `29705261453`에서 Linux의 Phase 2 interactive/build-model 검증과 Linux x64, macOS ARM64, Windows x64 build·binary smoke, 최종 release가 모두 success다. 실패한 phase5.1을 재사용하지 않고 새 phase5.2 전체 게이트로 다시 판정했다.
