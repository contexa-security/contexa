# 단계 6 상태전이·실서버·교차 플랫폼 완료 증거

판정일: 2026-07-19  
대응 백로그: SIBL-043~045  
대응 완료판정: SIAC-043~045  
현재 판정: **PASS — SIAC-043~045 전부 통과**

단계 6은 새 공개 명령이나 제품 기능을 추가하지 않았다. 기존 `init`, `reset`, `init --simulate`, `reset --simulate`의 상태 전이를 실제 자식 프로세스로 검증하고, dependency-only 호스트 보안 불변성, 명시적 simulation 인프라, 실제 릴리스 자산 설치·갱신·롤백 계약을 보강했다.

## 1. 배포 및 실행 식별자

- contexa-cli 최종 commit: `9989fc67109efc73c5eb23a743566db5858ab241`
- contexa-cli 최종 후보 tag: `v0.1.2-phase6.3`
- contexa-cli GitHub Actions run: `29707326350`
- contexa-cli 공개 prerelease: `356452761`
- install-ctxa 최종 commit: `a3b08ba394dc117a29415d126ee3d79670f13d3e`
- install-ctxa 최종 GitHub Actions run: `29707961048`, Windows·Ubuntu·macOS 모두 success
- starter version: `ai.ctxa:spring-boot-starter-contexa:0.1.0-SNAPSHOT`
- CLI package, release tag, release manifest CLI version: `0.1.2-phase6.3`으로 일치

## 2. SIAC-043 네 명령 상태 전이

판정: **PASS**

`test/phase6-state-transitions.test.js`가 임시 fixture에서 실제 CLI 자식 프로세스를 실행했다. 최종 결과는 7 PASS, 0 FAIL, 0 SKIP, 19.377초다.

| 번호 | 실제 상태 전이 | 검증 결과 |
| ---: | --- | --- |
| 01 | clean → 일반 init → 일반 reset | build·YAML digest 복구, manifest 제거, PASS |
| 02 | clean → simulation init → simulation reset | simulation 파일·소유 인프라 제거, PASS |
| 03 | 일반 init → simulation init → simulation reset → 일반 reset | 두 상태 독립 소유·역순 복구, PASS |
| 04 | simulation init → 일반 init → 일반 reset → simulation reset | 두 상태 독립 소유·역순 복구, PASS |
| 05 | 일반 init → 사용자 수정 → 일반 reset | 사용자 수정 3-way 보존, CLI 변경만 복구, PASS |
| 06 | simulation init → 사용자 수정 → simulation reset | 사용자 수정 3-way 보존, simulation 변경만 복구, PASS |
| 07 | 손상 YAML 실패 → `PARTIAL_FAILURE` → 동일 명령 재시도 → reset | 실패 manifest 보존, 원인 교정 후 복구, PASS |

각 결과는 `D:\tmp\phase6-state-evidence-6-3` 아래 JSON에 command, exit code, 상태 token, build·YAML SHA-256, manifest 상태, Docker inventory로 저장됐다.

macOS 게이트에서 발견된 `--no-docker` simulation reset 결함도 축소하지 않았다. `init --simulate --no-docker`가 `dockerLifecycleManaged=false`를 기록하도록 하고, reset은 소유 Docker 수명주기가 없으면 Docker CLI를 요구하지 않되 simulation 파일 3-way 복구와 manifest 제거를 수행한다. 해당 단일 회귀는 1 PASS, 전체 상태전이는 수정 후 7/7 PASS다.

## 3. SIAC-044 dependency-only 실제 Spring Boot 9080

판정: **PASS**

검증 fixture: `D:\tmp\contexa-phase2-host-20260719`  
포트: `9080`  
DB: PostgreSQL, H2 사용 안 함

설치 전·dependency-only init 후 동일한 실제 HTTP 요청을 비교했다.

| 항목 | 설치 전 | dependency-only 설치 후 | 판정 |
| --- | --- | --- | --- |
| 무인증 `/host/probe` | 401 | 401 | 동일 |
| Basic 인증 `/host/probe` | 200 | 200 | 동일 |
| principal | `host-admin` | `host-admin` | 동일 |
| authenticated | `true` | `true` | 동일 |
| authority | `ROLE_HOST_ADMIN` | `ROLE_HOST_ADMIN` | 동일 |
| Authentication 구현 | `UsernamePasswordAuthenticationToken` | 동일 | 동일 |
| SecurityFilterChain bean | `hostSecurityFilterChain` | 동일 | 동일 |
| 필터 수·배열 | 11, 동일 배열 | 11, 동일 배열 | 동일 |
| 활성 Contexa bean | 0 | 0 | 호스트에 무영향 |

CLI init은 build starter dependency만 추가했고 `application.yml` SHA-256은 바꾸지 않았다. reset 후 build와 YAML은 설치 전 digest로 복구됐고 normal manifest는 제거됐다. 시작 로그의 금지 패턴은 0건이며 시작 시간은 3.974초였다.

실제 PostgreSQL 증거는 health 정상, `public` table 302개, pgvector `0.8.2`다. 이는 현재 enterprise 포함 기존 DB를 관측한 값이며 OSS 설치가 302개 테이블을 생성했다는 의미가 아니다.

## 4. SIAC-044 명시적 simulation 실서버·DB·프로토콜

판정: **PASS**

`D:\contexa-started`에서 실제 simulation 인프라를 실행했다.

- PostgreSQL readiness와 실제 query: PASS
- Redis `PONG`: PASS
- Kafka `kafka-broker-api-versions`: exit 0
- Ollama 실제 tags: `qwen2.5:7b`, `mxbai-embed-large`
- 소유 container 5개: 모두 healthy
- 실제 서버 포트: 9080
- `/login`: 200
- OSS `public` table: 137개
- 로그 금지 패턴: 0건
- 일반 build·application YAML SHA-256: simulation 전후 동일

대표 인증 기능 200은 호스트 인증 fixture를 명시적 활성화한 별도 실행에서 확인했다.

| 요청·상태 | 실제 결과 |
| --- | --- |
| 무인증 `/host/probe` | 401 |
| Basic `host-admin` `/host/probe` | 200 |
| 인증된 `/actuator/health` | 200, `UP` |
| 호스트 principal·authority·Authentication·필터 | dependency-only baseline과 동일 |
| 명시적 활성 Contexa bean | 487 |
| PostgreSQL OSS table | 137 |

임시 fixture에는 공급자 capability를 명시하기 위해 기존 `contexa-started`와 동일한 provider dependency만 검증 중 추가했고, 검증 후 제거했다. 제품 소스나 사용자 기본 설정으로 편입하지 않았다.

종료 때 오래된 실패 실행 PID가 Geo DB 파일을 점유해 두 번 reset이 비정상 종료됐다. 이를 성공으로 계산하지 않았다. 정확한 `example.Phase2HostApplication` PID만 종료한 뒤 동일 reset을 다시 실행하여 simulation 파일·manifest·소유 container를 제거했다. 이후 normal reset으로 fixture build·YAML digest를 baseline과 일치시켰다. 최종 상태는 9080 listener 0, fixture Java process 0, simulation container 0이다.

주요 증거 파일:

- `D:\tmp\phase6-baseline-probe.json`
- `D:\tmp\phase6-host-security-comparison.json`
- `D:\tmp\phase6-postgresql-evidence.json`
- `D:\tmp\phase6-host-simulation-probe.json`
- `D:\tmp\phase6-host-simulation-health.json`
- `D:\tmp\phase6-host-sim-provider.out.log`
- `D:\tmp\phase6-acceptance-summary.json`

CLI·설치기 단계이므로 관리 HTML·JS 화면은 제품 계약 대상이 아니다. UI를 실행했다고 거짓 보고하지 않고 실제 HTTP·server·PostgreSQL·프로토콜 검증을 완료 근거로 사용한다.

## 5. SIAC-045 실제 릴리스 자산

판정: **PASS**

contexa-cli release run `29707326350`은 다음 네 job이 모두 success다.

- Linux x64 binary build·smoke
- macOS ARM64 binary build·smoke
- Windows x64 binary build·smoke
- release manifest·서명·9개 자산 게시

공개 prerelease는 2026-07-19T23:06:54Z에 게시됐다. 자산은 세 바이너리, 세 SHA-256, release manifest, RSA signature, public key의 정확히 9개다. 공개 manifest는 CLI `0.1.2-phase6.3`, tag `v0.1.2-phase6.3`, starter `0.1.0-SNAPSHOT`, 세 asset의 64자리 SHA-256, `RSA-3072-SHA256`을 포함한다. 다운로드한 manifest·signature·public key는 Node 표준 `crypto.verify`로 `RSA_SHA256_SIGNATURE_VALID`을 확인했다.

install-ctxa 로컬 변경 범위 테스트는 5 PASS, 0 FAIL, PowerShell 7 환경 부재 1 SKIP, 11.017초다. Windows 격리 디렉터리에서 실제 공개 자산으로 다음 순서를 완료했다.

1. `phase5.2` 신규 설치
2. `phase6.3` 갱신
3. `phase6.3` 동일 버전 재실행
4. rollback 후 실제 버전 `phase5.2` 확인
5. `phase6.3` 재설치와 실제 버전 확인
6. 공식 uninstall과 격리 디렉터리 제거

설치기 교차 OS workflow는 Ubuntu, macOS, Windows에서 실제 이전 prerelease 설치→현재 갱신→동일 버전→rollback→현재 재설치→실제 `init/reset` smoke를 실행한다. 최종 run `29707961048`의 `windows-latest`, `ubuntu-latest`, `macos-latest` 세 job이 모두 success다.

## 6. 실패 실행과 교정

실패한 실행은 완료 근거에서 제외했다.

- CLI `phase6.1` run `29706630462`: macOS 실패. 원인 진단 전 실행이므로 제외.
- CLI `phase6.2` run `29706870602`: `--no-docker` simulation reset이 Docker CLI를 무조건 요구. 소유 수명주기 metadata와 no-Docker reset 경로로 교정.
- install-ctxa run `29707435391`: Windows 실패. 진단 출력 부족으로 제외.
- install-ctxa run `29707513901`: Windows PowerShell이 상속된 `PSModulePath`에서 `Get-FileHash`를 못 찾음. .NET SHA-256 구현으로 교정.
- install-ctxa run `29707586423`: 동일 환경에서 Authenticode cmdlet 자동 로드 실패. `$PSHOME` 아래 Security module을 절대경로 import하고 정규 cmdlet로 `NotSigned`를 판정하도록 교정.
- install-ctxa run `29707698088`: macOS 공개 API rate limit과 Windows backup 직접 실행 결함. workflow token 인증을 추가하고 직접 실행할 수 없는 `.previous` 파일 검사를 실제 rollback 후 최종 실행 파일 버전 검사로 대체.

OpenSSL은 로컬에 없어 해당 명령 실패를 증거로 사용하지 않았다. 동일 공개 자산의 RSA-SHA256 검증은 Node 표준 crypto 결과만 완료 증거로 사용했다.

## 7. 제외한 대기 셀과 실패 명령

다음 셀은 `Script running with cell ID`를 반환해 즉시 종료했으며 PASS 증거에서 제외했다.

- `6304`: 결합 HTTP·로그 조회
- `6339`: 결합 protocol 조회
- `6471`: 광범위 reset 검색
- `6523`: 상세 git diff 조회
- `6543`: install-ctxa push

직접 임시 fixture에서 `simulate run`을 실행한 ENOENT, provider dependency가 없는 최소 fixture의 capability 실패, Geo DB 점유 중 reset 두 번도 PASS에 포함하지 않았다. 각각 원인을 제거한 독립 실행만 근거로 사용했다.

## 8. 최종 판정 조건

SIAC-043~045는 전부 PASS다. install-ctxa run `29707961048`의 Ubuntu·macOS·Windows job이 모두 success이며, 두 구현 저장소 작업 트리는 깨끗하고 로컬 격리 설치 파일은 공식 uninstall 뒤 제거됐다. 따라서 단계 6 완료판정기준을 100% 통과한 것으로 판정한다.
